#include <stdio.h>
/**
 * main - program that runs using printf and ends with a newline
 * return 0 (Sucess)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a work of art,\n");
return (0);
}
