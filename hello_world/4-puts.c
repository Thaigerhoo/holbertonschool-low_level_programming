#include <stdio.h>
/**
 * main  - program that prints a message and is followed by a  new line
 * Return: 0 (Sucess)
 */

int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
